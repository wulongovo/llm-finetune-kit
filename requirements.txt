# LLM Fine-tune Kit - Dependencies
# Optimized for RTX 3070 (8GB VRAM) + Windows

torch>=2.4.0
transformers>=4.45.0
peft>=0.13.0
trl>=0.12.0
datasets>=3.0.0
accelerate>=1.0.0
bitsandbytes>=0.44.0
scipy
sentencepiece
protobuf
tiktoken
gradio>=5.0.0
wandb
pandas
tqdm

# Install PyTorch with CUDA first:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
